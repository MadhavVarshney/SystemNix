export enum NewOld {
  "New" = 1,
  "Old" = 2
}

export enum Type {
  "Desktop" = 1,
  "Business Laptop" = 2,
  "Gaming Laptop" = 3
}

export enum StorageType {
  "Solid State Drive (SSD)" = 1,
  "Hard Disk Drtive (HDD)" = 2,
  "SSD + HDD" = 3
}

export enum GraphicsType {
  "Integrated" = 1,
  "Discrete" = 2
}
